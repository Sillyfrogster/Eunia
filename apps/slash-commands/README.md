# Slash commands

This bot shows a typed user option, an optional string option, a cooldown, and development-guild publishing.

## Run it

```sh
cp .env.example .env
bun install
bun run start
```

Set `DISCORD_TOKEN` and `DISCORD_GUILD_ID` before starting the bot. The example replaces the slash commands in that development server.
